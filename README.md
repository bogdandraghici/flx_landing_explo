# FlowX — landing explorations

Design explorations for the FlowX landing page.

| Version | Source | Live |
| --- | --- | --- |
| v1 | [`/v1`](v1/) | https://bogdandraghici.github.io/flx_landing_explo/v1/ |

Each `vN/` folder is a self-contained Vite project (`npm install && npm run dev`).
Deployed builds live in `docs/vN/` and are served by GitHub Pages.
